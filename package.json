{
  "name": "splatting-paint",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "3D painting on Gaussian Splats — standalone app built on the local PlayCanvas engine (no examples-browser UI).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@webxr-input-profiles/motion-controllers": "^1.0.0",
    "fflate": "0.8.2"
  }
}
