# Path to the LOCAL PlayCanvas engine source (src/index.js).
# This project aliases the bare `playcanvas` import to a beta engine checkout
# that contains GSplat features not present in the published npm package.
#
# Copy this file to `.env` and set the absolute path for your machine.
ENGINE_PATH=/abs/path/to/playcanvas-engine/src/index.js
